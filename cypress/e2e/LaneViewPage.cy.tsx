/**
 *
 * Open a live lane view.
 * The live view video stream appears in < 3 seconds.
 */
describe('Lane View Page (E2E)', () => {
    beforeEach('set up', () => {
        cy.visit('/lane-view');
    });

    it('live video stream plays < 3 seconds', () => {

    });
});