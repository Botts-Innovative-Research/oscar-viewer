/**
 * an event is selected, and the event preview opens up
 * an alarm is adjudicated
 */

describe('Chart - Event Preview (Component)', () => {
    beforeEach('Set up', () => {
        cy.visit('/dashboard');
    });

    it('Gamma charts appear with cps and threshold at the same time', () => {

    });

    it('Can toggle between nsigma and cps charts (Rapiscan only)', () => {

    });

    it('Rapiscan RPM chart displays correctly and timebar moves properly in sync with video', () => {

    });

    it('Aspect RPM chart displays correctly and timebar moves properly in sync with video', () => {

    });

});

describe('Chart - Event Details (Component)', () => {
    beforeEach('', () => {
        cy.visit('/event-details');
    });

    it('Gamma charts appear with cps and threshold at the same time', () => {

    });

    it('Can toggle between nsigma and cps charts (Rapiscan only)', () => {

    });

    it('Rapiscan RPM chart displays correctly and timebar moves properly in sync with video', () => {

    });

    it('Aspect RPM chart displays correctly and timebar moves properly in sync with video', () => {

    });

});