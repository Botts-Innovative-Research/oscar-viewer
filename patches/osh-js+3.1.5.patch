diff --git a/node_modules/osh-js/source/core/connector/MqttConnector.js b/node_modules/osh-js/source/core/connector/MqttConnector.js
index f49ff58..9665a61 100644
--- a/node_modules/osh-js/source/core/connector/MqttConnector.js
+++ b/node_modules/osh-js/source/core/connector/MqttConnector.js
@@ -56,23 +56,99 @@ class MqttConnector extends DataConnector {
         this.interval = -1;
         this.id = `mqtt-connector-${randomUUID()}`;
         this.mqttProvider = undefined;
+        this.topicSubscribers = new Map();
+        this.topicSubscriptionRequests = new Map();
+        this.messagePorts = new Map();
     }
 
     initBc() {
         this.onMessage = (data, topic) => {
-            this.broadcastChannel.postMessage({
-                data: data,
-                topic: topic
-            }, [data]);
+            const subscribers = this.topicSubscribers.get(topic);
+            if (!subscribers) {
+                return;
+            }
+            for (const connectorId of subscribers) {
+                const port = this.messagePorts.get(connectorId);
+                if (port) {
+                    port.postMessage({data: data, topic: topic});
+                }
+            }
         }
-        this.broadcastChannel = new BroadcastChannel(this.id);
-        this.broadcastChannel.onmessage = (message) => {
-            if(message.data.message === 'subscribe') {
-                this.doRequest(message.data.topic);
-            } else if(message.data.message === 'unsubscribe') {
-                this.disconnect(message.data.topic);
+    }
+
+    registerMessagePort(connectorId, port) {
+        const previousPort = this.messagePorts.get(connectorId);
+        if (previousPort) {
+            previousPort.close();
+            for (const [topic, subscribers] of this.topicSubscribers) {
+                subscribers.delete(connectorId);
+                if (subscribers.size === 0) {
+                    this.topicSubscribers.delete(topic);
+                    this.disconnect(topic);
+                }
             }
         }
+
+        this.messagePorts.set(connectorId, port);
+        port.onmessage = async (event) => {
+            if (event.data.message === 'subscribe') {
+                try {
+                    await this.subscribeTopic(event.data.topic, connectorId);
+                    port.postMessage({message: 'subscribe-ack', topic: event.data.topic});
+                } catch (error) {
+                    const message = error instanceof Error ? error.message : String(error);
+                    port.postMessage({
+                        message: 'subscribe-error',
+                        topic: event.data.topic,
+                        error: message
+                    });
+                }
+            } else if (event.data.message === 'unsubscribe') {
+                this.unsubscribeTopic(event.data.topic, connectorId);
+            }
+        };
+        port.start();
+    }
+
+    ensureConnected() {
+        this.getMqttProvider();
+    }
+
+    subscribeTopic(topic, connectorId) {
+        let subscribers = this.topicSubscribers.get(topic);
+        if (!subscribers) {
+            subscribers = new Set();
+            this.topicSubscribers.set(topic, subscribers);
+        }
+        if (subscribers.has(connectorId)) {
+            return this.topicSubscriptionRequests.get(topic) || Promise.resolve();
+        }
+        subscribers.add(connectorId);
+        if (subscribers.size === 1) {
+            const request = this.doRequest(topic)
+                .catch(error => {
+                    this.topicSubscribers.delete(topic);
+                    throw error;
+                })
+                .finally(() => {
+                    this.topicSubscriptionRequests.delete(topic);
+                });
+            this.topicSubscriptionRequests.set(topic, request);
+            return request;
+        }
+        return this.topicSubscriptionRequests.get(topic) || Promise.resolve();
+    }
+
+    unsubscribeTopic(topic, connectorId) {
+        const subscribers = this.topicSubscribers.get(topic);
+        if (!subscribers) {
+            return;
+        }
+        subscribers.delete(connectorId);
+        if (subscribers.size === 0) {
+            this.topicSubscribers.delete(topic);
+            return this.disconnect(topic);
+        }
     }
 
     getMqttProvider() {
@@ -119,7 +195,7 @@ class MqttConnector extends DataConnector {
     doRequest(topic = '',queryString= undefined) {
         const mqttProvider = this.getMqttProvider();
 
-        mqttProvider.subscribe(topic, this.onMessage).then(() => {
+        return mqttProvider.subscribe(topic, this.onMessage).then(() => {
             this.onChangeStatus(Status.CONNECTED);
         });
     }
diff --git a/node_modules/osh-js/source/core/connector/MqttTopicConnector.js b/node_modules/osh-js/source/core/connector/MqttTopicConnector.js
index eaedb0a..8ecb339 100644
--- a/node_modules/osh-js/source/core/connector/MqttTopicConnector.js
+++ b/node_modules/osh-js/source/core/connector/MqttTopicConnector.js
@@ -14,23 +14,85 @@ class MqttTopicConnector extends DataConnector {
         super(url, properties);
         this.lastReceiveTime = -1;
         this.interval = -1;
-        this.broadcastChannel = new BroadcastChannel(url);
+        this.broadcastChannel = null;
+        this.messagePort = properties.mqttOpts && properties.mqttOpts.messagePort;
+        this.pendingSubscriptions = new Map();
+        this.openBroadcastChannel();
+        this.topics = [];
+    }
+
+    openBroadcastChannel() {
+        if (this.messagePort) {
+            this.messagePort.onmessage = (message) => {
+                if(message.data.message === 'subscribe-ack') {
+                    this.clearSubscriptionRetry(message.data.topic);
+                } else if(message.data.message === 'subscribe-error') {
+                    this.clearSubscriptionRetry(message.data.topic);
+                    console.error(`MQTT subscription failed for ${message.data.topic}: ${message.data.error}`);
+                    this.onChangeStatus(Status.CLOSED_ERROR);
+                } else if(message.data.topic === this.fullTopic &&
+                    !message.data.message) {
+                    this.onMessage(message.data.data);
+                }
+            };
+            this.messagePort.start();
+            return;
+        }
+        if (this.broadcastChannel !== null) {
+            return;
+        }
+        this.broadcastChannel = new BroadcastChannel(this.url);
         this.broadcastChannel.onmessage = (message) => {
-            if(message.data.topic === this.fullTopic) {
+            if(message.data.message === 'subscribe-ack' &&
+                message.data.connectorId === this.id) {
+                this.clearSubscriptionRetry(message.data.topic);
+            } else if(message.data.topic === this.fullTopic &&
+                !message.data.message) {
                 this.onMessage(message.data.data);
             }
         }
-        this.topics = [];
     }
 
-    doRequest(topic = '',queryString= undefined) {
-        this.fullTopic = topic + '?'+queryString;
+    clearSubscriptionRetry(topic) {
+        const retry = this.pendingSubscriptions.get(topic);
+        if (retry) {
+            clearInterval(retry);
+            this.pendingSubscriptions.delete(topic);
+        }
+    }
+
+    requestSubscription(topic) {
+        if (this.messagePort) {
+            this.messagePort.postMessage({
+                message: 'subscribe',
+                connectorId: this.id,
+                topic: topic
+            });
+            return;
+        }
+        if (this.broadcastChannel === null) {
+            return;
+        }
         this.broadcastChannel.postMessage({
             message: 'subscribe',
             connectorId: this.id,
-            topic: this.fullTopic
+            topic: topic
         });
+    }
+
+    doRequest(topic = '',queryString= undefined) {
+        this.openBroadcastChannel();
+        this.fullTopic = topic + '?'+queryString;
+        if (this.topics.includes(this.fullTopic)) {
+            this.onChangeStatus(Status.CONNECTED);
+            return;
+        }
+        this.requestSubscription(this.fullTopic);
+        this.pendingSubscriptions.set(this.fullTopic, setInterval(() => {
+            this.requestSubscription(this.fullTopic);
+        }, 500));
         this.topics.push(this.fullTopic);
+        this.opened = true;
         this.onChangeStatus(Status.CONNECTED);
     }
     /**
@@ -45,8 +107,20 @@ class MqttTopicConnector extends DataConnector {
      * @param {Boolean} removeInterval  - force removing the interval
      */
     fullDisconnect(removeInterval) {
+        if (this.messagePort) {
+            for(let topic of this.topics) {
+                this.clearSubscriptionRetry(topic);
+                this.messagePort.postMessage({
+                    message: 'unsubscribe',
+                    connectorId: this.id,
+                    topic: topic,
+                });
+            }
+            this.topics = [];
+        }
         if (this.broadcastChannel != null) {
             for(let topic of this.topics) {
+                this.clearSubscriptionRetry(topic);
                 this.broadcastChannel.postMessage({
                     message: 'unsubscribe',
                     connectorId: this.id,
@@ -61,6 +135,7 @@ class MqttTopicConnector extends DataConnector {
             clearInterval(this.interval);
         }
         this.opened = false;
+        this.onChangeStatus(Status.DISCONNECTED);
     }
 
     /**
@@ -91,7 +166,7 @@ class MqttTopicConnector extends DataConnector {
     }
 
     isConnected() {
-        return this.broadcastChannel !== null && this.opened;
+        return (this.messagePort || this.broadcastChannel !== null) && this.opened;
     }
 
     checkStatus(status) {
diff --git a/node_modules/osh-js/source/core/datasource/DataSource.datasource.js b/node_modules/osh-js/source/core/datasource/DataSource.datasource.js
index 40240c1..4797403 100644
--- a/node_modules/osh-js/source/core/datasource/DataSource.datasource.js
+++ b/node_modules/osh-js/source/core/datasource/DataSource.datasource.js
@@ -135,7 +135,7 @@ class DataSource {
         return workersPool[dataSourceWorkers[this.id]];
     }
 
-    async initDataSource(properties = this.properties) {
+    async initDataSource(properties = this.properties, transfer = []) {
         return this.getWorker().postMessageWithAck({
             message: 'init',
             id: this.id,
@@ -144,7 +144,7 @@ class DataSource {
                 data: this.getTopicId()
             },
             dsId: this.id
-        }).then(() => {
+        }, transfer).then(() => {
             // listen for Events to callback to subscriptions
             const datasourceBroadcastChannel = new BroadcastChannel(this.getTopicId());
             datasourceBroadcastChannel.onmessage = (message) => {
diff --git a/node_modules/osh-js/source/core/datasource/TimeSeries.realtime.datasource.js b/node_modules/osh-js/source/core/datasource/TimeSeries.realtime.datasource.js
index e0cc528..514a2d5 100644
--- a/node_modules/osh-js/source/core/datasource/TimeSeries.realtime.datasource.js
+++ b/node_modules/osh-js/source/core/datasource/TimeSeries.realtime.datasource.js
@@ -38,10 +38,11 @@ function createSharedMqttConnector(properties, topic) {
     if(!(url in mqttConnectors)) {
         mqttConnectors[url] = new MqttConnector(url, properties);
         mqttConnectors[url].initBc();
+        mqttConnectors[url].ensureConnected();
     } else {
         console.log(`Reuse shared MqttConnector instance for ${url}`);
     }
-    return mqttConnectors[url].id;
+    return mqttConnectors[url];
 }
 
 class TimeSeriesRealtimeDatasource extends DataSource {
@@ -51,8 +52,9 @@ class TimeSeriesRealtimeDatasource extends DataSource {
         this.dataSynchronizer = undefined;
         this.properties.version = 0;
 
-        if(properties.protocol === 'mqtt') {
-            this.properties.mqttOpts.bcId = createSharedMqttConnector(properties);
+        if(properties.protocol === 'mqtt' && properties.mqttOpts.shared) {
+            this.sharedMqttConnector = createSharedMqttConnector(properties);
+            this.properties.mqttOpts.bcId = this.sharedMqttConnector.id;
         }
     }
 
@@ -256,8 +258,22 @@ class TimeSeriesRealtimeDatasource extends DataSource {
      * @protected
      * @param properties
      */
-    async initDataSource(properties) {
-        await super.initDataSource(properties);
+    async initDataSource(properties = this.properties) {
+        let workerProperties = properties;
+        let transfer = [];
+        if (properties.protocol === 'mqtt' && properties.mqttOpts.shared) {
+            const channel = new MessageChannel();
+            this.sharedMqttConnector.registerMessagePort(this.id, channel.port1);
+            workerProperties = {
+                ...properties,
+                mqttOpts: {
+                    ...properties.mqttOpts,
+                    messagePort: channel.port2
+                }
+            };
+            transfer = [channel.port2];
+        }
+        await super.initDataSource(workerProperties, transfer);
         const topics = {
             data: this.getTopicId(),
             time: this.getTimeTopicId()
diff --git a/node_modules/osh-js/source/core/mqtt/MqttProvider.js b/node_modules/osh-js/source/core/mqtt/MqttProvider.js
index f9a21db..1eff729 100644
--- a/node_modules/osh-js/source/core/mqtt/MqttProvider.js
+++ b/node_modules/osh-js/source/core/mqtt/MqttProvider.js
@@ -17,8 +17,6 @@
 import mqtt from './mqtt.min.js';
 import {isDefined} from "../utils/Utils.js";
 
-let mqttCallbacks = {};
-
 class MqttProvider {
     /**
      * Build the MqttProvider.
@@ -50,6 +48,7 @@ class MqttProvider {
         }
 
         this.topics = [];
+        this.mqttCallbacks = {};
 
         let options = {
             reconnectPeriod: 30,
@@ -93,22 +92,28 @@ class MqttProvider {
                     try {
                         // subscribe
                         // store callback for this topic
-                        if (!(topicQuery in mqttCallbacks)) {
-                            mqttCallbacks[topicQuery] = [];
+                        if (!(topicQuery in this.mqttCallbacks)) {
+                            this.mqttCallbacks[topicQuery] = [];
                         }
 
-                        mqttCallbacks[topicQuery].push({
+                        this.mqttCallbacks[topicQuery].push({
                             callbackFn: callback,
                             topic: topic
                         });
 
-                        this.client.subscribe(`${topicQuery}`, function (err) {
+                        this.client.subscribe(`${topicQuery}`, function (err, granted) {
                             if (err) {
-                                callback(err);
                                 error(err);
                             } else {
-                                console.warn(`Subscribed to ${topicQuery}`);
-                                resolve();
+                                const rejected = Array.isArray(granted) && granted.some(subscription =>
+                                    typeof subscription.qos === 'number' && subscription.qos >= 128);
+                                if (rejected) {
+                                    const subscriptionError = new Error(`Broker rejected MQTT subscription to ${topicQuery}`);
+                                    error(subscriptionError);
+                                } else {
+                                    console.info(`MQTT subscription active: ${topicQuery}`);
+                                    resolve(granted);
+                                }
                             }
                         });
                     } catch (exception) {
@@ -135,15 +140,19 @@ class MqttProvider {
      */
     async unsubscribe(topic) {
         const topicQuery = `${this.mqttPrefix}${topic}`;
-        return this.client.unsubscribe(topicQuery, {}, err => {
-            delete mqttCallbacks[topicQuery];
-            if (err) {
-                const messageErr = `Cannot Unsubscribed topic: ${topicQuery} : ${err}`;
-                console.error(messageErr);
-                throw Error(messageErr);
-            } else {
-                console.warn(`Unsubscribed topic: ${topicQuery}`);
-            }
+        delete this.mqttCallbacks[topicQuery];
+        this.topics = this.topics.filter(subscribedTopic => subscribedTopic !== topic);
+        return new Promise((resolve, reject) => {
+            this.client.unsubscribe(topicQuery, {}, err => {
+                if (err) {
+                    const messageErr = `Cannot unsubscribe topic: ${topicQuery}: ${err}`;
+                    console.error(messageErr);
+                    reject(new Error(messageErr));
+                } else {
+                    console.warn(`Unsubscribed topic: ${topicQuery}`);
+                    resolve();
+                }
+            });
         });
     }
 
@@ -164,6 +173,17 @@ class MqttProvider {
                 console.info(`Mqtt client is connected to ${that.endpoint}`);
             });
             this.client.on('message', this.onMessage.bind(this));
+            this.client.on('packetsend', packet => {
+                if (packet.cmd === 'subscribe') {
+                    const topics = packet.subscriptions.map(subscription => subscription.topic).join(', ');
+                    console.info(`MQTT SUBSCRIBE sent: ${topics}`);
+                }
+            });
+            this.client.on('packetreceive', packet => {
+                if (packet.cmd === 'suback') {
+                    console.info(`MQTT SUBACK received (messageId=${packet.messageId}, granted=${packet.granted.join(',')})`);
+                }
+            });
 
             this.client.on('reconnect', () => {
                 console.warn(`Mqtt client is attempting to reconnect to ${that.endpoint}`);
@@ -183,9 +203,9 @@ class MqttProvider {
     }
 
     async onMessage(topic, message) {
-        if (topic in mqttCallbacks) {
+        if (topic in this.mqttCallbacks) {
             // callback for the corresponding topic
-            for (let callback of mqttCallbacks[topic]) {
+            for (let callback of this.mqttCallbacks[topic]) {
                 // callback to all subscription registered
                 callback.callbackFn(new Uint8Array(message).subarray(message.byteOffset).buffer, callback.topic);
             }
@@ -198,7 +218,7 @@ class MqttProvider {
         }
         // close the client
         this.client.end();
-        mqttCallbacks = {};
+        this.mqttCallbacks = {};
         this.client = null;
     }
 
